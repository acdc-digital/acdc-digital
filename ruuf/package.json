{
  "name": "@acdc-digital/monorepo",
  "version": "1.0.0",
  "private": true,
  "packageManager": "pnpm@9.0.0",
  "scripts": {
    "dev": "pnpm --filter ruff dev",
    "convex": "pnpm --filter ruff convex:dev",
    "build": "pnpm --filter ruff build",
    "start": "pnpm --filter ruff start",
    "lint": "pnpm --filter ruff lint",
    "type-check": "pnpm --filter ruff type-check"
  },
  "devDependencies": {
    "@types/node": "^20",
    "typescript": "^5"
  }
}
